{
  "name": "didas",
  "version": "0.0.1",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "licence": "MIT",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --minify --dts --clean --sourcemap --splitting",
    "lint": "tsc"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3"
  }
}
